/**
 * @file Default application configuration at startup.
 */

const config = {
  buttons: {},
  io: {
    localFiles: true,
  },
  modes: {
    player: true,
    validation: true,
    vr: false,
  },
  preloadedShow: {},
  useWelcomeScreen: true,
};

export default config;
