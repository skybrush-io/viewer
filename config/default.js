/**
 * @file Default application configuration at startup.
 */

const config = {
  buttons: {},
  io: {
    localFiles: true,
  },
  modes: {
    player: true,
    validation: true,
  },
  preloadedShow: {},
  useWelcomeScreen: true,
};

export default config;
