/**
 * ID of the camera in the scene.
 *
 * This ID is used to find the tag representing the camera in the DOM.
 */
export const SCENE_CAMERA_ID = 'three-d-camera';

/**
 * CSS class to use for objects that can be selected in the scene.
 */
export const SELECTABLE_OBJECT_CLASS = 'three-d-selectable';
